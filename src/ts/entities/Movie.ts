export interface Movie {
  name: string;
  date: string;
  imgURL: string;
  trailerURL: string;
  nation: string;
  producer: string;
  directorList: string[];
  castList: string[];
  categoryList: string[];
  view: number;
  description: string;
}
